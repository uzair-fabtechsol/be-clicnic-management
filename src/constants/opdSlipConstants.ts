const VISIT_TYPES = ["new", "followUp"] as const;

const PAYMENT_METHODS = ["cash", "card", "bankTransfer"] as const;

const OPD_SLIP_NUMBER_DIGITS = 8;

export { VISIT_TYPES, PAYMENT_METHODS, OPD_SLIP_NUMBER_DIGITS };
